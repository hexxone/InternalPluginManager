<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.blockhaus2000</groupId>
	<artifactId>InternalPluginManager</artifactId>
	<version>1.9.2-SNAPSHOT</version>
	<packaging>jar</packaging>

	<!-- Some important project properties -->
	<properties>
		<!-- The encoding to use everywhere -->
		<project.properties.encoding>UTF-8</project.properties.encoding>

		<!-- The main class -->
		<project.properties.mainClass>com.blockhaus2000.main.bukkit.IpmMain</project.properties.mainClass>

		<!-- The Bukkit version to use -->
		<project.properties.bukkitVersion>1.7.9-R0.2</project.properties.bukkitVersion>

		<!-- The CraftBukkit jar name -->
		<project.properties.craftbukkitJarName>craftbukkit-1.7.9-R0.2-20140520.082451-2.jar</project.properties.craftbukkitJarName>

		<!-- Fix encoding warnings -->
		<project.build.sourceEncoding>${project.properties.encoding}</project.build.sourceEncoding>
	</properties>


	<!-- ===== Project Information ===== -->

	<name>InternalPluginManager</name>
	<inceptionYear>2014</inceptionYear>
	<url>https://www.github.com/Blockhaus2000/InternalPluginManager/</url>
	<description>An easy-to-use API that improves the command API from Bukkit. Also provides internal plugin management</description>

	<licenses>
		<license>
			<name>Apache License Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0</url>
		</license>
	</licenses>

	<issueManagement>
		<system>MantisBT</system>
		<url>http://issues.blockhaus2000.com/</url>
	</issueManagement>

	<scm>
		<connection>scm:git:git@github.com/Blockhaus2000/InternalPluginManager.git</connection>
		<developerConnection>scm:git:git@github.com:Blockhaus2000/InternalPluginManager.git</developerConnection>
		<url>https://www.github.com/Blockhaus2000/InternalPluginManager/</url>
	</scm>


	<!-- ===== Distribution Management ===== -->

	<distributionManagement>
		<!-- The site will be deployed here -->
		<!-- <site> <id>blockhaus2000-site-ftp</id> <url>ftp://docs.blockhaus2000.com/internalpluginmanager/</url> 
			</site> -->

		<!-- Releases will be deployed here -->
		<repository>
			<id>blockhaus2000-repo-ftp</id>
			<url>ftp://repo.blockhaus2000.com/</url>
		</repository>

		<!-- Snapshots will be deployed here -->
		<snapshotRepository>
			<id>blockhaus2000-snap-repo-ftp</id>
			<url>ftp://snap-repo.blockhaus2000.com/</url>
		</snapshotRepository>
	</distributionManagement>


	<!-- Plugin Repositories ===== -->

	<pluginRepositories>
		<pluginRepository>
			<id>blockhaus2000-plugin-repo</id>
			<url>http://repo.blockhaus2000.com/</url>
		</pluginRepository>
	</pluginRepositories>


	<!-- ===== Dependencies ===== -->

	<repositories>
		<repository>
			<id>bukkit-repo</id>
			<url>http://repo.bukkit.org/content/groups/public</url>
		</repository>
		<repository>
			<id>blockhaus2000-repo</id>
			<url>http://repo.blockhaus2000.com/</url>
		</repository>
	</repositories>

	<dependencies>
		<!-- Because this is a Bukkit plugin! -->
		<dependency>
			<groupId>org.bukkit</groupId>
			<artifactId>bukkit</artifactId>
			<version>${project.properties.bukkitVersion}</version>
			<scope>compile</scope>
		</dependency>

		<!-- Used to store serializable objects easily -->
		<dependency>
			<groupId>com.blockhaus2000</groupId>
			<artifactId>tag-storage-system</artifactId>
			<version>2.1</version>
		</dependency>

		<!-- Unit tests -->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.11</version>
			<scope>test</scope>
		</dependency>
	</dependencies>


	<!-- ===== Build ===== -->

	<build>
		<finalName>${project.artifactId}_v${project.version}-${timestamp}-${buildNumber}</finalName>
		<defaultGoal>clean install</defaultGoal>

		<directory>target</directory>
		<outputDirectory>${project.build.directory}/classes/</outputDirectory>
		<testOutputDirectory>${project.build.directory}/test-classes/</testOutputDirectory>
		<sourceDirectory>src/main/java/</sourceDirectory>
		<testSourceDirectory>src/test/java/</testSourceDirectory>

		<plugins>
			<!-- Compiler settings -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.1</version>
				<configuration>
					<compilerReuseStrategy>alwaysNew</compilerReuseStrategy>
					<compilerVersion>${project.properties.javaVersion}</compilerVersion>
					<debug>true</debug>
					<encoding>${project.properties.encoding}</encoding>
					<failOnError>true</failOnError>
					<forceJavacCompilerUse>true</forceJavacCompilerUse>
					<fork>true</fork>
					<maxmem>2048m</maxmem>
					<meminitial>64m</meminitial>
					<optimize>true</optimize>
					<showDeprecation>true</showDeprecation>
					<showWarnings>true</showWarnings>
					<skipMain>false</skipMain>
					<skipMultiThreadWarning>false</skipMultiThreadWarning>
					<source>${project.properties.javaVersion}</source>
					<target>${project.properties.javaVersion}</target>
					<useIncrementalCompilation>false</useIncrementalCompilation>
					<verbose>true</verbose>
				</configuration>
			</plugin>

			<!-- Used to deploy releases -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<version>2.8.1</version>
				<executions>
					<execution>
						<id>maven-deploy-plugin:deploy</id>
						<phase>deploy</phase>
						<goals>
							<goal>deploy</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<deployAtEnd>true</deployAtEnd>
					<retryFailedDeploymentCount>2</retryFailedDeploymentCount>
					<updateReleaseInfo>false</updateReleaseInfo>
				</configuration>
			</plugin>

			<!-- Generates JavaDoc -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>2.9.1</version>
				<executions>
					<execution>
						<id>maven-javadoc-plugin:javadoc</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>javadoc</goal>
						</goals>
					</execution>
					<execution>
						<id>maven-javadoc-plugin:jar</id>
						<phase>package</phase>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<outputDirectory>${project.build.directory}/apidocs/</outputDirectory>
					<applyJavadocSecurityFix>true</applyJavadocSecurityFix>
					<author>true</author>
					<bottom>Copytight &#169; {inceptionYear}&#x2013;{currentYear}
						Blockhaus2000. Licensed under ${licenses.license.name}
						(${licenses.license.url}).</bottom>
					<breakIterator>false</breakIterator>
					<charset>${project.properties.encoding}</charset>
					<debug>false</debug>
					<destDir>apidocs</destDir>
					<detectJavaApiLink>true</detectJavaApiLink>
					<detectLinks>true</detectLinks>
					<detectOfflineLinks>true</detectOfflineLinks>
					<docencoding>${project.properties.encoding}</docencoding>
					<docfilessubdirs>true</docfilessubdirs>
					<doctitle>${project.name} {project.version} API</doctitle>
					<encoding>${project.properties.encoding}</encoding>
					<failOnError>true</failOnError>
					<includeDependencySources>true</includeDependencySources>
					<includeTransitiveDependencySources>true</includeTransitiveDependencySources>
					<javadocVersion>${project.properties.javaVersion}</javadocVersion>
					<keywords>false</keywords>
					<linksource>true</linksource>
					<maxmemory>2048m</maxmemory>
					<nocomment>false</nocomment>
					<nodeprecatedlist>false</nodeprecatedlist>
					<nohelp>false</nohelp>
					<noindex>false</noindex>
					<nonavbar>false</nonavbar>
					<nooverview>false</nooverview>
					<nosince>false</nosince>
					<notimestamp>false</notimestamp>
					<notree>false</notree>
					<old>false</old>
					<packagesheader>Packages</packagesheader>
					<quiet>false</quiet>
					<serialwarn>true</serialwarn>
					<show>protected</show>
					<source>${project.properties.javaVersion}</source>
					<sourceDependencyCacheDir>${project.build.directory}</sourceDependencyCacheDir>
					<splitIndex>false</splitIndex>
					<stylesheet>maven</stylesheet>
					<use>true</use>
					<validateLinks>true</validateLinks>
					<verbose>false</verbose>
					<version>true</version>
					<windowtitle>${project.name} ${project.version} API</windowtitle>
					<finalName>${project.build.finalName}</finalName>
				</configuration>
			</plugin>

			<!-- Packages source code -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>2.3</version>
				<executions>
					<execution>
						<id>maven-source-plugin:source</id>
						<phase>generated-sources</phase>
						<goals>
							<goal>source</goal>
						</goals>
					</execution>
					<execution>
						<id>maven-source-plugin:jar</id>
						<phase>package</phase>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<attach>true</attach>
					<classifier>sources</classifier>
					<finalName>${project.build.finalName}</finalName>
					<forceCreation>true</forceCreation>
					<includePom>true</includePom>
					<outputDirectory>$[project.build.directory}</outputDirectory>
					<skipSource>false</skipSource>
					<useDefaultExcludes>true</useDefaultExcludes>
					<useDefaultManifestFile>false</useDefaultManifestFile>
				</configuration>
			</plugin>

			<!-- Test runner -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.17</version>
				<executions>
					<execution>
						<id>maven-surefire-plugin:test</id>
						<phase>test</phase>
						<goals>
							<goal>test</goal>
						</goals>
					</execution>
				</executions>
				<configuration> <!-- TODO -->
					<testSourceDirectory>${project.build.testSourceDirectory}</testSourceDirectory>
					<basedir>${basedir}</basedir>
					<childDelegation>true</childDelegation>
					<classesDirectory>${project.build.outputDirectory}</classesDirectory>
					<disableXmlReport>false</disableXmlReport>
					<enableAssertions>true</enableAssertions>
					<failIfNoSpecifiedTests>true</failIfNoSpecifiedTests>
					<failIfNoTests>true</failIfNoTests>
					<forkCount>2C</forkCount>
					<forkedProcessTimeoutInSeconds>5</forkedProcessTimeoutInSeconds>
					<parallelOptimized>true</parallelOptimized>
					<parallelTestsTimeoutForcedInSeconds>5</parallelTestsTimeoutForcedInSeconds>
					<parallelTestsTimeoutInSeconds>5</parallelTestsTimeoutInSeconds>
					<perCoreThreadCount>true</perCoreThreadCount>
					<printSummary>true</printSummary>
					<redirectTestOutputToFile>false</redirectTestOutputToFile>
					<remoteRepositories>${project.pluginArtifactRepositories}</remoteRepositories>
					<reportFormat>brief</reportFormat>
					<reportsDirectory>${project.build.directory}/surefire-reports/</reportsDirectory>
					<reuseForks>true</reuseForks>
					<runOrder>filesystem</runOrder>
					<testClassesDirectory>${project.build.testOutputDirectory}</testClassesDirectory>
					<testFailureIgnore>false</testFailureIgnore>
					<trimStackTrace>true</trimStackTrace>
					<useFile>true</useFile>
					<useManifestOnlyJar>true</useManifestOnlyJar>
					<useSystemClassLoader>true</useSystemClassLoader>
					<workingDirectory>${basedir}</workingDirectory>
				</configuration>
			</plugin>

			<!-- We want to test some commands within a Bukkit server -->
			<plugin>
				<groupId>com.blockhaus2000</groupId>
				<artifactId>bukkit-tester-maven-plugin</artifactId>
				<version>1.1.1</version>
				<executions>
					<execution>
						<id>bukkit-tester-maven-plugin:bukkit-test</id>
						<phase>integration-test</phase>
						<goals>
							<goal>bukkit-test</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<executionDirectory>${project.build.directory}/dist/</executionDirectory>
					<craftbukkitURL>http://repo.bukkit.org/content/repositories/public/org/bukkit/craftbukkit/${project.properties.bukkitVersion}/${project.properties.craftbukkitJarName}</craftbukkitURL>
					<craftbukkitFile>cache/${project.properties.craftbukkitJarName}</craftbukkitFile>
					<javaExecutionCommand>/usr/bin/java \
						-Dinternalpluginmanager.debug-mode=true -jar craftbukkit.jar</javaExecutionCommand>
					<failOnTestFailure>true</failOnTestFailure>
					<bukkitServerStartedIndicator>[START TESTS]</bukkitServerStartedIndicator>
					<testStartDelay>1000</testStartDelay>

					<plugins>
						<plugin>${project.build.directory}/${project.build.finalName}.jar</plugin>
					</plugins>
					<testCommands>
						<!-- Enable the test command output. This is not a real test. -->
						<testCommand>internalpluginmanager debug</testCommand>

						<testCommand>testcase1</testCommand>
						<testCommand>testcase2</testCommand>
						<testCommand>testcase3 arg0</testCommand>
						<testCommand>testcase4 arg0 arg1</testCommand>
						<testCommand>testcase5 arg0</testCommand>
						<testCommand>testcase6</testCommand>
						<testCommand>testcase7 -a</testCommand>
						<testCommand>testcase8 -b</testCommand>
						<testCommand>testcase9 -b flag-b</testCommand>
						<testCommand>testcase10 -c 123456</testCommand>
						<testCommand>testcase11 -c int</testCommand>
						<testCommand>testcase12 -d "Flag value of d"</testCommand>
						<testCommand>testcase13</testCommand>
						<testCommand>testcase14 arg0 123456</testCommand>
						<testCommand>testcase15 arg0 arg1 vararg0</testCommand>
						<testCommand>testcase16 arg0 123456 vararg0 vararg1 vararg2</testCommand>
					</testCommands>
					<successfulIndicators>
						<!-- This is our regular succeed indicator -->
						<successfulIndicator>[TEST SUCCESSFUL]</successfulIndicator>
					</successfulIndicators>
					<failureIndicators>
						<!-- This is our regular fail indicator -->
						<failureIndicator>[TEST FAILED]</failureIndicator>

						<!-- Also we want to mark a test as failed if an Exception occures -->
						<failureIndicator>Exception</failureIndicator>

						<!-- Also if a command cannot be found -->
						<failureIndicator>Unknown command.</failureIndicator>
					</failureIndicators>
				</configuration>
			</plugin>

			<!-- We do not want to depend on more than Bukkit! -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>2.3</version>
				<executions>
					<execution>
						<id>maven-shade-plugin:shade</id>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
					</execution>
				</executions>
				<configuration> <!-- TODO -->
					<artifactSet>
						<includes>
							<include>com.blockhaus2000:tag-storage-system</include>
						</includes>
					</artifactSet>
					<createDependencyReducedPom>true</createDependencyReducedPom>
					<createSourcesJar>false</createSourcesJar>
					<dependencyReducedPomLocation>${basedir}/dependency-reduced-pom.xml</dependencyReducedPomLocation>
					<minimizeJar>false</minimizeJar>
					<outputDirectory>${project.build.directory}</outputDirectory>
					<shadeSourcesContent>false</shadeSourcesContent>
					<shadeTestJar>false</shadeTestJar>
					<shadedArtifactAttached>false</shadedArtifactAttached>
					<shadedArtifactId>${project.artifactId}</shadedArtifactId>
					<shadedClassifierName>shaded</shadedClassifierName>
					<useBaseVersion>true</useBaseVersion>
				</configuration>
			</plugin>

			<!-- JAR plugin -->
			<plugin> <!-- TODO: Add executions -->
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>2.4</version>
				<configuration>
					<skipIfEmpty>true</skipIfEmpty>
					<archive>
						<addMavenDescriptor>true</addMavenDescriptor>
						<compress>false</compress>
						<forced>true</forced>
						<index>true</index>
						<manifest>
							<addClasspath>true</addClasspath>
							<addDefaultImplementationEntries>true</addDefaultImplementationEntries>
							<addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
							<addExtensions>true</addExtensions>
							<classpathLayoutType>repository</classpathLayoutType>
							<classpathMavenRepositoryLayout>true</classpathMavenRepositoryLayout>
							<mainClass>${project.properties.mainClass}</mainClass>
						</manifest>
					</archive>
				</configuration>
			</plugin>

			<!-- Replacement of some tags -->
			<plugin>
				<groupId>com.google.code.maven-replacer-plugin</groupId>
				<artifactId>replacer</artifactId>
				<version>1.5.3</version>
				<executions>
					<execution>
						<id>replacer:replace</id>
						<phase>prepare-package</phase>
						<goals>
							<goal>replace</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<file>src/main/resources/plugin.yml</file>
					<outputFile>${project.build.directory}/generated-resources/plugin.yml</outputFile>
					<regex>false</regex>
					<token>$project_version$</token>
					<value>${project.version}</value>
				</configuration>
			</plugin>

			<!-- Check style -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
				<version>2.12</version>
				<executions>
					<execution>
						<id>maven-checkstyle-plugin:check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
				<configuration> <!-- TODO -->
					<includeResources>true</includeResources>
					<includeTestResources>true</includeTestResources>
					<includes>**/*.java</includes>
					<resourceIncludes>**/*.*</resourceIncludes>
					<sourceDirectory>${project.build.sourceDirectory}</sourceDirectory>
					<cacheFile>${project.build.directory}/checkstyle-cachefile/</cacheFile>
					<configLocation>checkstyle.xml</configLocation>
					<consoleOutput>false</consoleOutput>
					<encoding>${project.properties.encoding}</encoding>
					<failOnViolation>true</failOnViolation>
					<failsOnError>false</failsOnError>
					<includeTestSourceDirectory>true</includeTestSourceDirectory>
					<logViolationsToConsole>true</logViolationsToConsole>
					<maxAllowedViolations>0</maxAllowedViolations>
					<outputFile>${project.build.directory}/checkstyle-result.xml</outputFile>
					<outputFileFormat>xml</outputFileFormat>
					<testSourceDirectory>${project.build.testSourceDirectory}</testSourceDirectory>
					<violationSeverity>error</violationSeverity>
				</configuration>
			</plugin>

			<!-- Find TODO's --> <!-- //TODO-FINDER:IGNORE -->
			<plugin>
				<groupId>com.blockhaus2000</groupId>
				<artifactId>todo-finder-maven-plugin</artifactId>
				<version>1.4.1</version>
				<executions>
					<execution>
						<id>todo-finder-maven-plugin:find</id>
						<phase>verify</phase>
						<goals>
							<goal>find</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<lowPriorityStrings>
						<lowPriorityString>IDEA</lowPriorityString> <!-- //TODO-FINDER:IGNORE -->
					</lowPriorityStrings>
					<normalPriorityStrings>
						<normalPriorityString>TODO</normalPriorityString> <!-- //TODO-FINDER:IGNORE -->
					</normalPriorityStrings>
					<highPriorityStrings>
						<highPriorityString>FIXME</highPriorityString> <!-- //TODO-FINDER:IGNORE -->
					</highPriorityStrings>
					<failOnHighPriority>true</failOnHighPriority>
					<todoListFile>${project.build.directory}/todo-finder-report.txt</todoListFile>
					<excludeFiles>
						<excludeFile>.git/</excludeFile>
						<excludeFile>dist/</excludeFile>
						<excludeFile>target/</excludeFile>
						<excludeFile>dependency-reduced-pom.xml</excludeFile>
					</excludeFiles>
				</configuration>
			</plugin>

			<!-- Generate build numbers -->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>buildnumber-maven-plugin</artifactId>
				<version>1.3</version>
				<executions>
					<execution>
						<id>buildnumber-maven-plugin:create</id>
						<phase>initialize</phase>
						<goals>
							<goal>create</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<buildNumberPropertyName>buildNumber</buildNumberPropertyName>
					<doCheck>false</doCheck>
					<doUpdate>false</doUpdate>
					<getRevisionOnlyOnce>false</getRevisionOnlyOnce>
					<shortRevisionLength>5</shortRevisionLength>
					<timestampPropertyName>timestamp</timestampPropertyName>
					<useLastCommittedRevision>false</useLastCommittedRevision>
				</configuration>
			</plugin>

			<!-- Used to automate the JavaDoc upload -->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>wagon-maven-plugin</artifactId>
				<version>1.0-beta-5</version>
				<!-- We only want do deploy the JavaDoc HTML source for a release! -->
				<!--     Moved to profile configuration -->
				<!--
				<executions>
					<execution>
						<id>wagon-maven-plugin:upload</id>
						<phase>deploy</phase>
						<goals>
							<goal>upload</goal>
						</goals>
					</execution>
				</executions>
				-->
				<configuration> <!-- TODO -->
					<url>ftp://docs.blockhaus2000.com</url>
					<followSymLink>false</followSymLink>
					<fromDir>${project.build.directory}/apidocs/</fromDir>
					<optimize>false</optimize>
					<serverId>blockhaus2000-docs-ftp</serverId>
					<toDir>internalpluginmanager/${project.version}</toDir>
					<useDefaultExcludes>true</useDefaultExcludes>
				</configuration>
			</plugin>

			<!-- Used for release automation -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-release-plugin</artifactId>
				<version>2.5</version>
				<!-- Executions will be performed in the profiles -->
				<configuration>
					<javaHome>${java.home}</javaHome>
					<localRepoDirectory>${maven.repo.local}</localRepoDirectory>
					<mavenExecutorId>invoker</mavenExecutorId>
					<mavenHome>${maven.home}</mavenHome>
					<pomFileName>pom.xml</pomFileName>
					<addSchema>true</addSchema>
					<allowTimestampedSnapshots>false</allowTimestampedSnapshots>
					<autoVersionSubmodules>true</autoVersionSubmodules>
					<commitByProject>false</commitByProject>
					<preparationGoals>clean verify</preparationGoals>
					<pushChanges>true</pushChanges>
					<remoteTagging>false</remoteTagging>
					<resume>false</resume>
					<scmCommentPrefix>[maven-release-plugin]</scmCommentPrefix>
					<suppressCommitBeforeTag>true</suppressCommitBeforeTag>
					<tagNameFormat>@{project.version}</tagNameFormat>
					<updateDependencies>false</updateDependencies>
					<updateWorkingCopyVersion>true</updateWorkingCopyVersion>
					<useEditMode>false</useEditMode>
					<username>Blockhaus2000</username>
					<goals><!--deploy-->install</goals> <!-- TODO -->
					<localCheckout>false</localCheckout>
				</configuration>
			</plugin>

			<!-- Site builder -->
			<!--<plugin> <groupId>org.apache.maven.plugins</groupId> <artifactId>maven-site-plugin</artifactId> 
				<version>3.1</version> <configuration> -->
			<!-- Project information -->
			<!--<plugin> <groupId>org.apache.maven.plugins</groupId> <artifactId>mavan-project-info-reports-plugin</artifactId> 
				<version>2.5</version> <reportSets> <reportSet> <reports> <report>license</report> 
				<report>index</report> <report>javadoc</report> </reports> </reportSet> </reportSets> 
				</plugin> -->

			<!-- JavaDoc -->
			<!--<plugin> <groupId>org.apache.maven.plugins</groupId> <artifactId>mavan-javadoc-plugin</artifactId> 
				<version>2.8.1</version> </plugin> </configuration> </plugin> -->
		</plugins>

		<resources>
			<resource>
				<directory>src/main/resources/</directory>
				<excludes>
					<exclude>plugin.yml</exclude> <!-- We do not want to include the raw plugin.yml -->
				</excludes>
			</resource>
			<resource>
				<directory>${project.build.directory}/generated-resources/</directory>
				<includes>
					<include>plugin.yml</include> <!-- We want to use the plugin.yml with the corrent project version -->
				</includes>
			</resource>
		</resources>

		<!-- Connecter to the deploy repository -->
		<extensions>
			<extension>
				<groupId>org.apache.maven.wagon</groupId>
				<artifactId>wagon-ftp</artifactId>
				<version>2.5</version>
			</extension>
		</extensions>
	</build>


	<!-- ===== Profiles ===== -->

	<profiles>
		<profile>
			<id>release</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<build>
				<defaultGoal>release:clean release:prepare release:perform</defaultGoal>

				<plugins>
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>wagon-maven-plugin</artifactId>
						<version>1.0-beta-5</version>
						<executions>
							<execution>
								<id>wagon-maven-plugin:upload</id>
								<phase>deploy</phase>
								<goals>
									<goal>upload</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>

		<profile>
			<id>snapshot</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<build>
				<defaultGoal>clean <!--deploy-->install</defaultGoal>
			</build>
		</profile>
	</profiles>
</project>
